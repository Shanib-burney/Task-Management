version: '3.8'

services:
  app:
    build: .
    ports:
      - "3050:3000"
    environment:
      DB_HOST: postgre
      DB_PORT: 5432
      DB_USERNAME: xplorie
      DB_PASSWORD: mypassword#123
      DB_NAME: task-management-gql
      DB_SCHEMA: public
      NODE_ENV: development
      PRISMA_HIDE_PARAMETERS: true
    networks:
      - environment_xnet

networks:
  environment_xnet:
    external: true