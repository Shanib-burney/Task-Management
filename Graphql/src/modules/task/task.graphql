type Task {
  id: ID!
  title: String!
  description: String
  status: Int!
}

input CreateTaskInput {
  title: String!
  description: String
  projectId: Int!
  assigneeId: Int
}

type Mutation {
  createTask(input: CreateTaskInput!): Task!
}
