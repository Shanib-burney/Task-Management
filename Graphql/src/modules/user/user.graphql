type User {
  id: ID!
  name: String!
  email: String!
  ownedProjects: [Project!]!
}

input CreateUserInput {
  name: String!
  email: String!
  password: String!
}

type Query {
  users: [User!]!
}

type Mutation {
  createUser(input: CreateUserInput!): User!
}
